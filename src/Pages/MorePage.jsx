const MorePage = ({ onBackButtonClicked }) => {
  return (
    <div>
      <div>No more haha</div>
      <button onClick={onBackButtonClicked}>Go back</button>
    </div>
  );
};

export default MorePage;
